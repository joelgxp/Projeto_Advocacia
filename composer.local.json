{
    "name": "advocacia/sistema-dev",
    "description": "Configuração de desenvolvimento para o Sistema de Advocacia",
    "require-dev": {
        "phpunit/phpunit": "^9.0",
        "symfony/var-dumper": "^5.0",
        "monolog/monolog": "^2.0"
    },
    "scripts": {
        "dev": "php -S localhost:8000 -t public/",
        "test": "phpunit",
        "cs": "phpcs --standard=PSR12 src/",
        "cs-fix": "phpcbf --standard=PSR12 src/"
    }
}

