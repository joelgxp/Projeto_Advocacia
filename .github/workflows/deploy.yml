name: Build and Deploy via SSH

on:
  push:
    branches:
      - main

jobs:
  build-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
    
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
    
      - name: Install NPM dependencies
        run: npm ci
    
      - name: Build assets
        run: npm run build
    
      - name: Verify build output
        run: |
          if [ -d "public/build" ]; then
            echo "✅ Build concluído com sucesso"
            ls -la public/build/
          else
            echo "❌ Erro: public/build não foi criado"
            exit 1
          fi
    
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
    
      - name: Check for changes in compiled assets
        id: check-changes
        run: |
          if [ -n "$(git status --porcelain public/build/ 2>/dev/null)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Alterações detectadas em public/build/"
            git status --short public/build/
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "Nenhuma alteração em public/build/"
          fi
    
      - name: Commit compiled assets
        if: steps.check-changes.outputs.has_changes == 'true'
        run: |
          git add public/build/
          git commit -m "chore: atualizar assets compilados [skip ci]" || exit 0
          git push origin HEAD:main || exit 0
          echo "✓ Arquivos compilados commitados e enviados para o repositório"

  deploy:
    needs: build-and-commit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH with password
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Upload diagnostic scripts to server
        run: |
          echo "📤 Enviando scripts de diagnóstico para o servidor..."
          sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "mkdir -p ${{ secrets.SSH_DIRECTORY }}/scripts" || true
          
          sshpass -p "${{ secrets.SSH_PASSPHRASE }}" scp -P ${{ secrets.SSH_PORT }} \
            scripts/check-server.sh \
            scripts/check-laravel-online.sh \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_DIRECTORY }}/scripts/ || {
            echo "⚠️ Erro ao enviar scripts, tentando método alternativo..."
            exit 1
          }

      - name: Run server diagnostic
        id: diagnostic
        continue-on-error: true
        run: |
          echo "🔍 Executando diagnóstico no servidor..."
          
          # Executar scripts de diagnóstico no servidor
          echo "Executando diagnóstico geral..."
          sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.SSH_DIRECTORY }} && \
            chmod +x scripts/check-server.sh scripts/check-laravel-online.sh 2>/dev/null && \
            echo '=== DIAGNÓSTICO GERAL ===' && \
            bash scripts/check-server.sh 2>&1" || echo "⚠️ Erro ao executar diagnóstico geral"
          
          echo ""
          echo "Executando diagnóstico Laravel online..."
          sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.SSH_DIRECTORY }} && \
            echo '' && echo '=== DIAGNÓSTICO LARAVEL ONLINE ===' && \
            bash scripts/check-laravel-online.sh 2>&1" || echo "⚠️ Erro ao executar diagnóstico Laravel online"
          
          # Aguardar um momento para os arquivos serem criados
          sleep 3
          
          # Buscar e baixar os relatórios mais recentes
          REPORT_FILE=$(sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.SSH_DIRECTORY }} && ls -t diagnostico-*.txt 2>/dev/null | head -1" || echo "")
          
          LARAVEL_REPORT=$(sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.SSH_DIRECTORY }} && ls -t diagnostico-laravel-online-*.txt 2>/dev/null | head -1" || echo "")
          
          # Combinar relatórios
          echo "📥 Baixando relatórios..."
          if [ -n "$REPORT_FILE" ] && [ "$REPORT_FILE" != "" ]; then
            sshpass -p "${{ secrets.SSH_PASSPHRASE }}" scp -P ${{ secrets.SSH_PORT }} \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_DIRECTORY }}/$REPORT_FILE \
              diagnostic-report.txt 2>/dev/null || true
          fi
          
          if [ -n "$LARAVEL_REPORT" ] && [ "$LARAVEL_REPORT" != "" ]; then
            sshpass -p "${{ secrets.SSH_PASSPHRASE }}" scp -P ${{ secrets.SSH_PORT }} \
              ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_DIRECTORY }}/$LARAVEL_REPORT \
              diagnostic-laravel-online.txt 2>/dev/null || true
            
            # Combinar relatórios
            if [ -f "diagnostic-report.txt" ] && [ -f "diagnostic-laravel-online.txt" ]; then
              echo "" >> diagnostic-report.txt
              echo "==========================================" >> diagnostic-report.txt
              echo "  DIAGNÓSTICO LARAVEL ONLINE (ESPECÍFICO)" >> diagnostic-report.txt
              echo "==========================================" >> diagnostic-report.txt
              echo "" >> diagnostic-report.txt
              cat diagnostic-laravel-online.txt >> diagnostic-report.txt
            elif [ -f "diagnostic-laravel-online.txt" ]; then
              cp diagnostic-laravel-online.txt diagnostic-report.txt
            fi
          fi
          
          # Verificar se o relatório existe e tem conteúdo
          if [ -f "diagnostic-report.txt" ] && [ -s "diagnostic-report.txt" ]; then
            LINE_COUNT=$(wc -l < diagnostic-report.txt)
            echo "✅ Relatório preparado ($LINE_COUNT linhas)"
          else
            echo "⚠️ Relatório vazio ou não encontrado"
            echo "Verificando estrutura do servidor..." > diagnostic-report.txt
            sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
              "cd ${{ secrets.SSH_DIRECTORY }} && pwd && ls -la | head -20" >> diagnostic-report.txt 2>&1 || true
          fi

      - name: Display diagnostic summary
        if: always()
        continue-on-error: true
        run: |
          SUMMARY_FILE="$GITHUB_STEP_SUMMARY"
          
          # Função helper para adicionar ao summary
          add_to_summary() {
            echo "$1" >> "$SUMMARY_FILE"
          }
          
          add_to_summary "## 🔍 Diagnóstico do Servidor"
          add_to_summary ""
          add_to_summary "**Data:** $(date '+%Y-%m-%d %H:%M:%S')"
          add_to_summary "**Servidor:** ${{ secrets.SSH_HOST }}"
          add_to_summary ""
          
          if [ -f "diagnostic-report.txt" ] && [ -s "diagnostic-report.txt" ]; then
            # Verificar problemas críticos do Laravel online
            HAS_APP_KEY_ISSUE=$(grep -qi "APP_KEY.*não está configurado\|APP_KEY não configurado" diagnostic-report.txt 2>/dev/null && echo "true" || echo "false")
            HAS_STORAGE_LOGS_ISSUE=$(grep -qi "storage/logs.*NÃO existe" diagnostic-report.txt 2>/dev/null && echo "true" || echo "false")
            HAS_COMPOSER_ISSUE=$(grep -qi "Composer.*não instalado" diagnostic-report.txt 2>/dev/null && echo "true" || echo "false")
            HAS_INDEX_ISSUE=$(grep -qi "public/index.php.*NÃO existe\|index.php NÃO existe" diagnostic-report.txt 2>/dev/null && echo "true" || echo "false")
            HAS_HTACCESS_ISSUE=$(grep -qi "public/.htaccess.*NÃO existe\|.htaccess NÃO existe" diagnostic-report.txt 2>/dev/null && echo "true" || echo "false")
            
            if [ "$HAS_APP_KEY_ISSUE" = "true" ] || [ "$HAS_STORAGE_LOGS_ISSUE" = "true" ] || [ "$HAS_INDEX_ISSUE" = "true" ] || [ "$HAS_HTACCESS_ISSUE" = "true" ] || [ "$HAS_COMPOSER_ISSUE" = "true" ]; then
              add_to_summary "### ⚠️ Itens que Precisam de Atenção"
              add_to_summary ""
              
              if [ "$HAS_APP_KEY_ISSUE" = "true" ]; then
                add_to_summary "❌ **CRÍTICO: APP_KEY não está configurado!**"
                add_to_summary "   Execute: php artisan key:generate"
                add_to_summary ""
              fi
              if [ "$HAS_INDEX_ISSUE" = "true" ]; then
                add_to_summary "❌ **CRÍTICO: public/index.php não existe!**"
                add_to_summary ""
              fi
              if [ "$HAS_HTACCESS_ISSUE" = "true" ]; then
                add_to_summary "❌ **CRÍTICO: public/.htaccess não existe!**"
                add_to_summary ""
              fi
              if [ "$HAS_STORAGE_LOGS_ISSUE" = "true" ]; then
                add_to_summary "⚠️ **storage/logs** não existe"
              fi
              if [ "$HAS_COMPOSER_ISSUE" = "true" ]; then
                add_to_summary "⚠️ **Composer** não está no PATH (mas vendor/ existe, dependências instaladas)"
              fi
              
              add_to_summary ""
            else
              add_to_summary "### ✅ Nenhum Item Crítico Encontrado"
              add_to_summary ""
            fi
            
            # Resumo executivo
            add_to_summary "### 📊 Resumo Executivo"
            add_to_summary ""
            
            # Verificar APP_KEY (CRÍTICO)
            if grep -qi "APP_KEY.*está configurado" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **APP_KEY:** Configurado"
            elif [ "$HAS_APP_KEY_ISSUE" = "true" ]; then
              add_to_summary "❌ **APP_KEY:** NÃO configurado (CRÍTICO!)"
            else
              add_to_summary "⚠️ **APP_KEY:** Não verificado"
            fi
            
            # Verificar .env
            if grep -qi "Arquivo .env existe" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **Arquivo .env:** Existe"
            elif grep -qi "Arquivo .env NÃO existe" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "❌ **Arquivo .env:** NÃO existe"
            else
              add_to_summary "ℹ️ **Arquivo .env:** Não verificado"
            fi
            
            # Verificar bootstrap/cache
            if grep -qi "bootstrap/cache existe" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **bootstrap/cache:** Existe"
            else
              add_to_summary "⚠️ **bootstrap/cache:** Não verificado"
            fi
            
            # Verificar storage/framework
            if grep -qi "storage/framework existe" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **storage/framework:** Existe"
            else
              add_to_summary "⚠️ **storage/framework:** Não verificado"
            fi
            
            # Verificar storage/logs
            if grep -qi "storage/logs existe" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **storage/logs:** Existe"
            elif [ "$HAS_STORAGE_LOGS_ISSUE" = "true" ]; then
              add_to_summary "⚠️ **storage/logs:** NÃO existe"
            else
              add_to_summary "ℹ️ **storage/logs:** Não verificado"
            fi
            
            # Verificar vendor
            if grep -qi "Diretório vendor/" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **vendor/:** Existe"
            else
              add_to_summary "⚠️ **vendor/:** Não verificado"
            fi
            
            # Verificar PHP
            PHP_LINE=$(grep -i "PHP.*[0-9]" diagnostic-report.txt 2>/dev/null | grep -i "versão\|version" | head -1)
            if [ -z "$PHP_LINE" ]; then
              PHP_LINE=$(grep -i "PHP [0-9]" diagnostic-report.txt 2>/dev/null | head -1)
            fi
            if [ -n "$PHP_LINE" ]; then
              PHP_VERSION=$(echo "$PHP_LINE" | grep -oE "PHP [0-9]+\.[0-9]+\.[0-9]+" | head -1 | sed 's/PHP //')
              if [ -n "$PHP_VERSION" ]; then
                add_to_summary "✅ **PHP:** $PHP_VERSION"
              else
                add_to_summary "✅ **PHP:** Instalado"
              fi
            else
              add_to_summary "⚠️ **PHP:** Não detectado"
            fi
            
            # Verificar Composer
            if grep -qi "Composer instalado" diagnostic-report.txt 2>/dev/null; then
              add_to_summary "✅ **Composer:** Instalado"
            elif [ "$HAS_COMPOSER_ISSUE" = "true" ]; then
              if grep -qi "Diretório vendor/" diagnostic-report.txt 2>/dev/null; then
                add_to_summary "⚠️ **Composer:** Não no PATH (mas dependências instaladas)"
              else
                add_to_summary "❌ **Composer:** Não instalado"
              fi
            else
              add_to_summary "ℹ️ **Composer:** Não verificado"
            fi
            
            add_to_summary ""
            add_to_summary "### 🔧 Comandos para Corrigir Problemas"
            add_to_summary ""
            
            # Comandos baseados nos problemas
            if [ "$HAS_APP_KEY_ISSUE" = "true" ]; then
              add_to_summary "**🔴 CRÍTICO: Gerar APP_KEY (OBRIGATÓRIO):**"
              add_to_summary '```bash'
              add_to_summary "php artisan key:generate"
              add_to_summary '```'
              add_to_summary ""
            fi
            if [ "$HAS_STORAGE_LOGS_ISSUE" = "true" ]; then
              add_to_summary "**Criar diretório storage/logs:**"
              add_to_summary '```bash'
              add_to_summary "mkdir -p storage/logs && chmod -R 775 storage"
              add_to_summary '```'
              add_to_summary ""
            fi
            
            add_to_summary "**Comandos Gerais do Laravel:**"
            add_to_summary '```bash'
            add_to_summary "# Limpar e regenerar cache"
            add_to_summary "php artisan config:clear"
            add_to_summary "php artisan config:cache"
            add_to_summary "php artisan route:cache"
            add_to_summary "php artisan view:cache"
            add_to_summary '```'
            add_to_summary ""
            
            # Relatório completo
            REPORT_LINES=$(wc -l < diagnostic-report.txt | tr -d ' ')
            add_to_summary "### 📄 Relatório Completo"
            add_to_summary ""
            add_to_summary "<details>"
            add_to_summary "<summary>🔽 Clique para expandir o relatório completo ($REPORT_LINES linhas)</summary>"
            add_to_summary ""
            add_to_summary '```'
            cat diagnostic-report.txt >> "$SUMMARY_FILE"
            add_to_summary '```'
            add_to_summary ""
            add_to_summary "</details>"
            add_to_summary ""
            add_to_summary "💡 **Dica:** O relatório completo também está disponível como artifact para download."
          else
            add_to_summary "### ⚠️ Relatório não disponível"
            add_to_summary ""
            add_to_summary "O relatório de diagnóstico não foi gerado ou está vazio."
            add_to_summary ""
            add_to_summary "**Possíveis causas:**"
            add_to_summary "- Erro de conexão SSH"
            add_to_summary "- Script de diagnóstico não foi executado"
            add_to_summary "- Problemas de permissão no servidor"
          fi
          
          # Log preview no console
          echo ""
          echo "=========================================="
          echo "  PREVIEW DO DIAGNÓSTICO"
          echo "=========================================="
          if [ -f "diagnostic-report.txt" ] && [ -s "diagnostic-report.txt" ]; then
            REPORT_LINES=$(wc -l < diagnostic-report.txt | tr -d ' ')
            echo "Primeiras 30 linhas:"
            head -30 diagnostic-report.txt
            echo ""
            echo "... (relatório completo: $REPORT_LINES linhas)"
            echo "✅ Ver relatório completo no GitHub Actions Summary ou no artifact"
          else
            echo "⚠️ Relatório não disponível"
          fi

      - name: Upload diagnostic report as artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: diagnostic-report-${{ github.run_number }}
          path: diagnostic-report.txt
          retention-days: 30
          if-no-files-found: warn

      - name: Git pull on server
        run: |
          sshpass -p "${{ secrets.SSH_PASSPHRASE }}" ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.SSH_DIRECTORY }} && \
            if [ ! -d .git ]; then \
              echo '❌ Erro: Diretório não é um repositório Git. Verifique o caminho em SSH_DIRECTORY.'; \
              echo 'Diretório atual:' && pwd && ls -la; \
              exit 1; \
            fi && \
            echo '📥 Fazendo git fetch...' && \
            git fetch origin && \
            echo '🔄 Fazendo git pull...' && \
            git pull origin main && \
            echo '✅ Deploy concluído com sucesso!' && \
            echo '' && \
            echo '📦 Arquivos atualizados:' && \
            ls -la public/build/ 2>/dev/null || echo '⚠️ public/build não encontrado'"